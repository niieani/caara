# Slice 09: Antigravity portable delegation

## Outcome

Portable delegation now has deterministic Antigravity proof through the public CLI adapter, real
HTTP router, session directory/concurrency lifecycle, real Antigravity driver, and a fake `agy`
executable/transcript boundary.

## Contract proved

- first turn and resume preserve the external Antigravity conversation;
- caller wait results contain final answers only while capability viewer HTML contains normalized
  Antigravity reasoning and activity;
- cwd, model-family effort mapping, sandbox posture, timeout, prompt, and resume cursor reach the
  spawned process boundary;
- same-session overlap is rejected while a resumed turn is active;
- cancellation after transcript mutation is `Terminated` and non-reusable;
- real credentials and installed services remain behind `CAARA_REAL_AGY_SMOKE=1`.

Operator procedure: `docs/agents/antigravity-portable-smoke-runbook.md`.
