# CAARA-tlyjkhtm

Implement Antigravity validation failures as invalid-prompt surfaced failures.

Tests first:

- option parser invalid cases assert exact message and `responseErrorCode = invalid_prompt`
- prompt extraction failures assert exact message and `invalid_prompt`
- provider invalid Antigravity option emits SSE `response.failed` `invalid_prompt`, no completion

Implementation:

- Antigravity option/prompt validation uses shared invalid-prompt builder from slice 1
- keep CLI process/transcript/runtime failures explicit `server_error`

Evidence:

- `bun run test --run src/antigravityCliDriver/options.test.ts src/antigravityCliDriver/prompt.test.ts src/antigravityCliDriver/antigravityCliDriver.test.ts`
  - 3 files, 33 tests
