# CAARA-muroxffy

Configure checked-in `caara-claude` role for scoped smoke writes.

Role policy:

- `permission_mode = "dontAsk"`
- `additional_directories = "$TMPDIR"`
- allow writes/edits under `$TMPDIR/caara-panel/smoke/**`
- no `Bash(...)` approval; the smoke prompt passes the expanded absolute TMPDIR path
- no `bypassPermissions`

Smoke docs/evidence:

- role query params
- command/prompt used
- created file evidence
- relay-log outcome with no required-write `PermissionDenied`
