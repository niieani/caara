# Slice 10: Codex portable guidance

Issue: CAARA-eyscstvm

## Outcome

The existing Caara-marked global Codex `AGENTS.md` block now teaches the complete portable blind
workflow: target/cwd selection, safe prompt files, accepted start, immediate human URL surfacing,
strict non-inspection, repeated bounded waits, cancellation, session reuse, and final-only result
consumption. It explicitly does not depend on native Codex subagent roles or schemas.

Existing marker-aware install/update/uninstall behavior preserves unrelated user guidance. The
Current authenticated smoke procedures are centralized in `docs/agents/smoke-testing.md`.
