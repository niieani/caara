# CAARA-izqiluwf

Make pre-driver validation failures Codex-readable.

Tests first:

- current-turn normalization failure streams `response.failed` with `invalid_prompt`
- unsupported external agent kind streams `response.failed` with `invalid_prompt`
- both paths preserve existing exact message and avoid HTTP `server_error`

Design:

- request-shape / metadata validation remains HTTP 400 `invalid_request_error`
- accepted turn failures before driver start use SSE `response.failed`
- no lease acquired until registry resolution and input normalization pass

Evidence:

- `bun run test --run src/mockResponsesProvider/mockResponsesProvider.test.ts src/mockResponsesProvider/agentRegistryRouting.test.ts`
  - 2 files, 13 tests
- `bun run typecheck`
- `bun lint`
- `bun run test --run`
