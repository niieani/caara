# CAARA-jwebokyx

Implement Claude driver scoped TMPDIR expansion.

Red tests:

- `additional_directories` parses comma-delimited values and maps to SDK `additionalDirectories`.
- `$TMPDIR` and `${TMPDIR}` work.
- `allowed_tools` and `disallowed_tools` expand only rule specifiers and produce `//absolute`
  Claude syntax.
- Missing, empty, relative TMPDIR, and unsupported env placeholders fail explicitly.
- `AskUserQuestion` rejection remains.

Docs:

- `docs/caara.md` Claude query options.
