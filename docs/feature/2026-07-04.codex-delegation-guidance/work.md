# Work plan: codex-delegation-guidance

## Guidance block content (final copy)

```markdown
## Cross-model subagents (Caara)

`caara-*` subagent roles run external code agents (Claude Code, Antigravity) as native Codex
subagents — a genuinely different model family, not another Codex seat. Prefer them over native
subagents when a different model's perspective adds value: code review, second opinions,
contested design calls, or tasks that play to another model's strengths. Follow-up turns resume
the same external session, so treat them as persistent collaborators rather than one-shot tools.
```

Plus, only when panel skill present:

```markdown
For structured multi-model work — ensemble attempts, debates, cross-review — invoke the global
`$panel` skill.
```

Markers: `<!-- caara:agents:begin -->` / `<!-- caara:agents:end -->` (+ "Generated by Caara. Do
not edit between markers." note inside begin comment line kept short).

## New/changed files

- `src/panelSkillAssets.ts` — `panelSkillAssets: Record<relPath, string>` via 5 md text imports.
- `src/markdownModules.d.ts` — `declare module "*.md"` default string.
- `vitest.config.ts` — inline `markdownTextPlugin` (transform: id endsWith `.md` → `export default JSON.stringify(code)`).
- `src/codexAgentsGuidance.ts` — pure: `renderCodexAgentsGuidanceBlock({panelSkillInstalled})`,
  `upsertCodexAgentsGuidance({source, block})`, `removeCodexAgentsGuidance({source})` returning
  `{content, changed}`-ish; fs effects: `writeCodexAgentsGuidanceFile`, `removeCodexAgentsGuidanceFile`.
- `src/codexSkillInstaller.ts` — `installPanelSkill({codexHome})` / `removePanelSkill({codexHome})`;
  marker file `.caara-generated` content `caara-generated-skill = true`; collision → error result
  mirroring role collision message style.
- `src/codexRoleInstallerOptions.ts` — add `agentsMd: boolean`, `panelSkill: boolean` parsing
  (`--agents-md`, `--panel-skill`), keep them out of targetArgs.
- `src/codexRoleInstaller.ts` — orchestrate after successful role write: install skill (if flag),
  then agents-md upsert (if flag; panel presence checked after skill step). Extend
  install/uninstall results (`writtenFiles`/`removedFiles` + message lines). Uninstall always
  cleans marked skill + managed block. `defaultCodexHomeDirectory({env})` exported; agents dir
  derives from it.
- Tests: `codexAgentsGuidance.test.ts`, `codexSkillInstaller.test.ts`,
  `panelSkillAssets.test.ts` (sync w/ `.agents/skills/panel/**`), extend
  `codexRoleInstaller.test.ts` + new `codexRoleInstallerGuidance.test.ts` for flag flows,
  `caaraCli` untouched (flags pass through args).
- `README.md` — new "Teach Codex to delegate" section after Quickstart: flags + manual snippet.
- `site/src/components/sections/Quickstart.astro` — 4th step with
  `caara install-codex-roles --agents-md --panel-skill`.

## Slices

1. `1.guidance-block` — pure block module + fs effects + tests (red first).
2. `2.skill-assets` — d.ts, vitest plugin, assets module, sync test, skill installer + tests.
3. `3.cli-flags` — options parsing, orchestration in installer, uninstall cleanup, tests.
4. `4.docs` — README + site + binary smoke (build:service, run against temp CODEX_HOME).

## Status

All 4 slices done. 52 tests green (guidance, skill installer, assets sync, opt-in flows, existing
role installer suites, CLI dispatch, service roles). Typecheck + lint clean. Binary smoke:
build:service → install/uninstall with both flags against temp CODEX_HOME verified (embedded
assets work compiled). Site builds.

Deviations from plan:
- Marker/dir/filename string consts became nullary functions; `if`/ternaries/nested Effect.gen
  swept out — repo's @catenarycloud/linteffect grit rules forbid them (one diagnostic surfaces
  per file per rule, so sweep fully). codexRoleToml's `caaraGeneratedCodexRoleMarker()` was the
  precedent.
- Roles-dir mkdir moved from command entry into successfulInstallResult so refusals
  (role/skill collision, corrupt AGENTS.md) write nothing at all.
- uninstall-codex-roles cleans the marked skill + managed block whenever CODEX_HOME/HOME
  resolves, regardless of explicit target-dir arg (marked-artifacts-only, safe).

## Notes

- Effect style: Effect.fnUntraced, no try/catch, Match/Option patterns as in codexRoleInstaller.
- Message lines: `installed panel skill to <dir>`, `updated Codex AGENTS.md guidance at <path>`,
  `removed panel skill from <dir>`, `removed Codex AGENTS.md guidance from <path>`.
- Flags w/o CODEX_HOME/HOME → exitCode 1 message mirroring existing "requires ... HOME or
  CODEX_HOME must be set."
