# CAARA-lioqnzlx

Implement Claude driver invalid-prompt surfaced failures.

Tests first:

- provider SSE invalid Claude option returns `response.failed` `invalid_prompt` and exact message
- TMPDIR/permission validation errors preserve exact messages and return `invalid_prompt`
- unsupported Claude prompt content returns `invalid_prompt`
- expected SDK startup failures return `invalid_prompt`

Implementation:

- Claude validation helpers already use shared invalid-prompt builder from slice 1
- add missing provider-level assertions and refine startup classification if needed

Result:

- Added provider SSE assertions for unsupported `permission-mode`, bad
  `additional_directories=$HOME/caara`, unsupported `input_audio`, and native SDK startup failure.
- Added focused settings assertion for missing `claude` executable as `invalid_prompt`.
- Added direct option assertions covering `additional_directories`, `allowed_tools`, and
  `disallowed_tools` scoped validation as `invalid_prompt`.
- validation: `bun lint`; `bun run typecheck`; `bun run test --run`; `bun run fmt`; focused Claude
  tests.
