# CAARA-ytjzxyeh

Document Codex-visible driver failure semantics and smoke paths.

Docs:

- `docs/caara.md`: accepted driver/request failures use SSE `response.failed` with
  `invalid_prompt`; internal retryable failures opt into `server_error`
- `docs/agents/diagnostic-smoke-runbooks.md`: dedicated `caara-diagnostic-fails-invalid-request`
  smoke
- `docs/agents/smoke-testing.md`: Claude invalid-option failure canary with exact message
- historical docs with old `server_error` evidence marked superseded

Verification:

- doc lint by review / `rg`
- focused test references included for AFK verification:
  - `src/mockResponsesProvider/mockResponsesProvider.test.ts`
  - `src/claudeAgentSdkDriver/claudeAgentSdkActivity.test.ts`
- `bun lint`
- `bun run test --run`
- `bun run typecheck`
- `git diff --check`
